            // [columnComment]
            if(searchMap.get("[column]")!=null && !"".equals(searchMap.get("[column]"))){
                wrapper.like("[column]","%"+searchMap.get("[column]")+"%");
           	}